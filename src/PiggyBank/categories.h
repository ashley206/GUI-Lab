#ifndef CATEGORIES_H
#define CATEGORIES_H
#include "category.h"
class Categories
{
public:
    Categories();
    AddCategory(char * name);
    RemoveCategory(Category c);
private:

};

#endif // CATEGORIES_H
